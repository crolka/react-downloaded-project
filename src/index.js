import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App'; // this is the App.js file reference.  the .js extension should be left off.

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
// jsx 
// App is a component.